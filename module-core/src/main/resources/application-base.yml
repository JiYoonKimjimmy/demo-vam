# server
server:
  shutdown: graceful
  servlet:
    context-path: /vam
  tomcat:
    threads:
      min-spare: 10
      max: 200
    connection-timeout: 20s

# springdoc
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    doc-expansion: none

# actuator
management:
  endpoints:
    web:
      base-path: /
      exposure:
        include: health, shutdown, metrics, prometheus
  endpoint:
    health:
      enabled: true
      show-details: always
    shutdown:
      enabled: true
  health:
    redis:
      enabled: false
    rabbit:
      enabled: false

# masking
masking:
  fields: FIELD|token|6|4, FIELD|aspId|4|4, FIELD|di|0|0, FIELD|ci|0|0, FIELD|mpaId|0|0, FIELD|address|2|0, FIELD|mobileTokenList|2|0, FIELD|cardInfoList|2|0

# encrypt cubeone
database:
  encrypt-provider: CUBE_ONE

# spring
spring:
  datasource:
    hikari:
      pool-name: hikaricp-pool
      driver-class-name: oracle.jdbc.driver.OracleDriver
      minimum-idle: 5
      maximum-pool-size: 20
      max-lifetime: 60000
      idle-timeout: 30000
      connection-timeout: 10000
      connection-test-query: SELECT 1 FROM DUAL
  jpa:
    database: oracle

# logging
logging:
  level:
    com.zaxxer.hikari.pool.HikariPool: DEBUG
    org.apache.hc.client5.http: DEBUG

---

spring:
  config:
    activate:
      on-profile: dev
  datasource:
    driver-class-name: oracle.jdbc.driver.OracleDriver
    url: jdbc:oracle:thin:@118.33.122.24:15322:KMDBP19
    username: vam
    password: StgVAM1104#
  jpa:
    generate-ddl: true
    properties:
      hibernate:
        ddl-auto: update
        format_sql: true
        show_sql: true
  data:
    redis:
      timeout: 10s
      connect-timeout: 20s
      password: 123456
      sentinel:
        master: mymaster
        nodes:
          - 118.33.122.28:26379
      lettuce:
        pool:
          min-idle: 1
          max-idle: 8
          max-active: 30